rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // In the AI Studio preview, we allow open access for easy testing.
      // ALWAYS harden these rules in a production environment.
      allow read, write: if true;
    }
  }
}
